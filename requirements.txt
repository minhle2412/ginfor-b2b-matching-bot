# B2B Matching — Python dependencies
# Cài đặt:  pip install -r requirements.txt

# --- Matching engine ---
sentence-transformers>=2.2.0
numpy>=1.24.0

# --- Nguồn dữ liệu Supabase (gọi PostgREST trực tiếp, không cần supabase-py) ---
requests>=2.31.0
python-dotenv>=1.0.0

# --- API + dashboard ---
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# --- Bot Facebook / Discord ---
discord.py>=2.3.0
beautifulsoup4>=4.12.0
pyppeteer>=1.0.2

# --- Dịch bài đăng buyer Anh → Việt (model NMT offline) ---
transformers>=4.30.0
torch>=2.0.0
sentencepiece>=0.2.0       # bắt buộc cho MarianTokenizer (opus-mt)
sacremoses>=0.1.0

# --- B2B Harvester scrapers ---
lxml>=4.9.0
playwright>=1.40.0
